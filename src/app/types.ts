type Portfolio = {
  title: string;
  href: string;
  preview?: string;
  description?: string;
  comment?: string;
};

type SocialLink = {
  title: string;
  href: string;
  icon: React.ReactNode;
};